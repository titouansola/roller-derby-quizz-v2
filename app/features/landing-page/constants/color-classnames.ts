export const colorClassnames = {
  dark: 'bg-dark text-light',
  primary: 'bg-primary text-dark',
  secondary: 'bg-secondary text-dark',
};

export type ColorClassName = keyof typeof colorClassnames;
